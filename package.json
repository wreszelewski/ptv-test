{
  "name": "ptv-tests",
  "version": "1.0.0",
  "description": "Test suite for automatic tests of programtv.onet.pl website",
  "main": "index.js",
  "author": "Wojciech Reszelewski",
  "license": "ISC",
  "scripts": {
    "test": "./node_modules/moca/bin/moca",
    "postinstall": "cd node_modules && git clone https://github.com/wreszelewski/zombie.git && cd zombie && npm install && npm run build"
  },
  "dependencies": {
    "mocha": "^3.0.2"
  }
}
